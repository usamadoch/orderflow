# MongoDB Quarantine

This folder contains the old MongoDB driver and repository code. It has been moved here to safely remove it from the active codebase while keeping it around for reference until the TimescaleDB migration is fully verified in production.

Once TimescaleDB is fully tested and verified, this directory can be safely deleted.
